import parameters

# parse parameters

# initialize environment
# initialize network
# initialize agent
# initialize monitor

# loop max iterations
    # action, q_values = agent.get_action(state)
    # state, reward, terminal = environment.act(action)
    # agent.after_action(state, reward, action, terminal)

    # monitor.monitor(state, reward, terminal, q_values)

    # if terminal:
        # reset